# CI Status

_This page is automatically updated by CI with test results and benchmark data._

_Run the CI workflow or generate locally with:_

```bash
uv run python scripts/generate_summary.py \
    --artifacts-dir ./artifacts \
    --output docs/ci-status.md
```
